{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/a5345534/goal-dag/schemas/model-catalog.schema.json",
  "title": "Goal DAG Model Class Catalog",
  "type": "object",
  "additionalProperties": false,
  "required": ["modelRouting"],
  "properties": {
    "modelRouting": {
      "type": "object",
      "additionalProperties": false,
      "required": ["rules"],
      "properties": {
        "controllerScenario": { "type": "string", "minLength": 1 },
        "defaultSubagentScenario": { "type": "string", "minLength": 1 },
        "rules": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/rule" }
        }
      }
    }
  },
  "$defs": {
    "conditionScalar": {
      "oneOf": [
        { "type": "string", "minLength": 1 },
        { "type": "number" },
        { "type": "boolean" }
      ]
    },
    "conditionValue": {
      "oneOf": [
        { "$ref": "#/$defs/conditionScalar" },
        { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/conditionScalar" } }
      ]
    },
    "rule": {
      "type": "object",
      "additionalProperties": false,
      "required": ["when", "modelScenario", "modelClass"],
      "properties": {
        "when": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/$defs/conditionValue" }
        },
        "modelScenario": { "type": "string", "minLength": 1 },
        "modelClass": { "type": "string", "pattern": "^[a-z][a-z0-9]*(?:[-_.][a-z0-9]+)*$" }
      }
    }
  }
}
